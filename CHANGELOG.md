# 更新记录

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.2] - 2026-09-24

### 变更

- **收窄权限**：移除 `host_permissions: ["*://*/*"]` 与 `tabs` 权限。
  移除悬浮球后，扩展不再需要访问任何网站，也不再需要读取标签页信息，
  现在只剩 4 个功能必需的权限：

  | 权限 | 用途 |
  | --- | --- |
  | `storage` | 保存音箱 IP 等设置 |
  | `alarms` | 定时保活、断线后重连 |
  | `offscreen` | 在屏幕外文档里长期维持 WebSocket 连接 |
  | `system.network` | 「自动查找音箱」时获取本机网段 |

- 面板复用改为使用 `runtime.getContexts()` 判断（它不需要任何权限，也不受
  标签页 URL 读取限制影响），不再依赖 `tab.url`

> **升级后请确认音箱仍能连上。** WebSocket 连接局域网地址在部分 Chrome 版本上
> 受「本地网络访问」策略影响，之前是在拥有 `*://*/*` 权限的情况下验证通过的。
> 万一连不上，设置页的「后台诊断」会显示 `WebSocket 建立 0 次`，告诉我即可。

## [1.0.1] - 2026-09-24

### 移除

- **网页悬浮球功能整体移除**（含迷你 HUD、设置里的开关、注入任意网页的内容脚本）。
  它需要在所有网页上运行、并在每个标签页里保留一份界面实例，对一个「调音箱音量」的
  工具来说收益不足。现在扩展只在你打开界面时才工作，权限与资源占用都更干净：
  - `manifest.json` 不再声明 `content_scripts`
  - 删除 `src/content/`（`content.js`、`content.css`）
  - 设置页去掉「在网页右下角显示悬浮球」开关
  - service worker 的状态广播不再遍历所有标签页（改为只发给扩展页面）

> 如果你正在用旧版的悬浮球：升级后它会消失，这是预期行为。
> 旧配置里遗留的 `showOrb` 字段会被忽略，不影响其它设置。

## [1.0.0] - 2026-09-24

首个可用版本。

### 功能

- 与 R1 音箱（EchoService，`ws://<IP>:8080`）建立连接，浏览器启动后自动重连，图标角标实时显示音量
- 工具栏弹窗：圆形旋钮（拖动 / 滚轮 / 方向键 / 双击静音）、滑杆、预设档位、步长切换、静音开关
- 悬浮控制面板（独立置顶窗口，可拖动，含完整设备信息与「重启服务」）
- 可选网页悬浮球：任意网页右下角实时音量环 + 迷你 HUD
- 设置页：手动填写 IP、同网段自动扫描并握手验证、测试连接、音量上限、配置导入导出
- 键盘快捷键：打开面板、音量 ±、静音
- 全局唯一：连接与指令队列都收敛到唯一的后台连接桥，多个界面不会互相打架

### 健壮性

- 连接跑在屏幕外文档（offscreen document）中，不受 service worker 30 秒空闲回收影响
- 断线指数退避重连；`chrome.alarms` 定期保活与探活
- 写音量后以设备回读值为准；一次拖动/按键只下发一条指令（`pointerup` 与 `change` 去重）
- 静音：优先写到 0，固件不接受时自动探测其最低档并记住，再点一次恢复原音量
- 连接桥自检与「重建连接桥」按钮；设置页内置后台诊断（含启动过程日志）

### 兼容性说明

- 实测某些 Chrome 版本的屏幕外文档里 `chrome.storage` 为 `undefined`（`chrome.runtime` 正常）。
  连接桥的启动门槛因此只看 `chrome.runtime`，`storage` 缺失时降级为向 service worker 取设置。
- 官方协议没有静音指令，静音通过写入最低档实现；若固件把 0 钳到 1，会把该档位记为静音档。
